var Messaging;
(function (Messaging) {
    var Modal = (function () {
        function Modal(id) {
            this.Id = id;
        }
        return Modal;
    }());
    Messaging.Modal = Modal;
})(Messaging || (Messaging = {}));
