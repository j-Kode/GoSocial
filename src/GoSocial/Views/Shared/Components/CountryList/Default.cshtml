@model IEnumerable<GoSocial.Models.Country>

@Html.DropDownList("Country", new SelectList(Model.Select(x => new { Value = x.Id, Text = x.Country1}), "Value", "Text"), "select country", new {@class="form-control"})