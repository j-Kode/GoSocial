@model IEnumerable<GoSocial.Models.Platform>

@Html.DropDownList("PlatformId", new SelectList(Model.Select(x => new { Value = x.Id, Text = x.Platform1 }), "Value", "Text"), "select campaign platform", new {@class="form-control" } )